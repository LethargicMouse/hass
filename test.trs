fn main(args) {
  trust(args)
}

fn trust(args) {
  process(args)
}

fn process(args) {
  if args.len != 20 {
    die("! expected exactly one argument")
  }
}

fn die(str) {
  @print(str)
}

struct slice {
  len
}

type args = slice;

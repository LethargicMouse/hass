fn main(args) {
  trust(args)
}

fn trust(args) {
  process(args)
}

fn process(args) {
  if args.len != 1 {
    die("! expected exactly one argument\n")
  }
  run_file(args.buf[1], path)
}

fn run_file() {
  
}

fn die(str) {
  @print(str)
}

struct slice {
  len
}

type args = slice;
